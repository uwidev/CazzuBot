services:
  database:
    image: postgres:16
    environment:
      POSTGRES_DB: main
      POSTGRES_USER: cazzubot
      POSTGRES_PASSWORD: f00b@rz
    # POSTGRES_PASSWORD_FILE: /run/secrets/secret
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    secrets:
      - secret
    command: -p 5432

  # webapp:
  #   build: .
  #   stdin_open: true
  #   tty: true
  #   depends_on:
  #     - database
  #   volumes:
  #     - botlog:/mnt/bot_log
  #   # command: -d -s
  #   command: -d
  #   environment:
  #     POSTGRES_DB: main
  #     POSTGRES_USER: cazzubot
  #     POSTGRES_PASSWORD_FILE: /run/secrets/secret
  #     POSTGRES_IP: 192.168.1.3
  #     TOKEN_FILE: /run/secrets/token
  #     OWNER_ID: 92664421553307648
  #     LOG_PATH: /mnt/bot_log
  #     POSTGRES_IP_DEV: 192.168.1.119
  #     TOKEN_FILE_DEV: /run/secrets/token_dev
  #   secrets:
  #     - secret
  #     - token
  #     - token_dev
  #   # cap_add:
  #   #   - sys_ptrace

  # pgadmin:
  #   image: dpage/pgadmin4
  #   ports:
  #     - "8080:80"
  #   environment:
  #     PGADMIN_DEFAULT_EMAIL: cazzubot@foo.bar
  #     PGADMIN_DEFAULT_PASSWORD_FILE: /run/secrets/secret
  #   volumes:
  #     - pgadmin:/var/lib/pgadmin
  #   secrets:
  #     - secret
 
  # pyspy:
  #   build:
  #     context: pyspy/
  #   pid: "host"
  #   privileged: "true"
  #   volumes:
  #     - .:/profiles

volumes:
  pgdata:
  botlog:
  pgadmin:

secrets:
  secret:
    file: ./secret/secret
  token:
    file: ./secret/token
  token_dev:
    file: ./secret/token_dev
